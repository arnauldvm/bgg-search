# feat/exceptions — plan

## Steps

1. Add `BggSearchError` (base) to `src/bgg_search/exceptions.py` + unit tests in `tests/unit/test_exceptions.py`.
2. Add `BggApiError` to `src/bgg_search/exceptions.py` + unit tests.
3. Add `BggNotFoundError` to `src/bgg_search/exceptions.py` + unit tests.
4. Add `BggParseError` to `src/bgg_search/exceptions.py` + unit tests.
