# Modification 3 — `CHANGELOG.md`

Single step (one commit).

## Step 1 — `CHANGELOG.md`

Create `CHANGELOG.md` in Keep a Changelog (v1.0.0) format:
- Standard header with format and semver links.
- Empty `## [Unreleased]` section — no entries yet.

First user-facing entries will be added in phase 0.4.

`tox` not yet set up; skip quality gate for this step.
