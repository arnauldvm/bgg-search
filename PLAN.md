# Modification 20 — Post-release version bump

## Goal

Immediately bump the version to 0.2.0.dev0 after tagging 0.1.0, so the dev branch
never sits at a released version number.

## Steps

1. `pyproject.toml` — set version to `0.2.0.dev0`
