# Plan: BggClient.get_game

## Step 1 — `BggClient.get_game` + unit tests + CHANGELOG

Files:
- `src/bgg_search/_client.py` — add `get_game` method
- `tests/unit/test_client.py` — add `get_game` tests
- `tests/integ/test_search_flow.py` — add `get_game` integ test
- `CHANGELOG.md` — `Added` entry under `[Unreleased]`
