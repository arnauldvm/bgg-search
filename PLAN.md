# Plan — Docstrings on all public symbols

## Steps

1. ✓ Domain types: class-level and field-level docstrings for `GameSummary`, `GameDetails`
   (`models.py`) and all exception classes (`exceptions.py`).
2. API layer: docstrings for `BggClientProtocol` and its two methods (`_protocol.py`),
   and for `search_games` and `get_game` (`search.py`).
