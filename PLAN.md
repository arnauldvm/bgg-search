# Plan: BggClient.search

## Step 1 — `BggClient` with `search` + unit tests

Files:
- `src/bgg_search/_client.py` — new; `BggClient.__init__` and `BggClient.search`
- `tests/unit/test_client.py` — new; four unit tests for `search`
